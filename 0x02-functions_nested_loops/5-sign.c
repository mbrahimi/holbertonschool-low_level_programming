#include "main.h"
#include <ctype.h>

/**
 * print_sign (int n) - check the code 
 *
 * Return: can change when n is differant 
 */
 int print_sign(int n);
 if ( n > 0)
 return (1)
 if ( n = 0 )
 return (0)
 if ( n < 0 )
return (-1)
