#include "main.h"
#include <stdio.h>
/**
* 
*
* Return: Always 0 (Success)
*/
int main(void)
{
char low, 2, 4;
e = '2';
q = '4';
for (low = '0'; low <= '9'; low++)
{
if (low != 2 && low != 4)
_putchar(low);
}
_putchar('\n');
return (0);
}
