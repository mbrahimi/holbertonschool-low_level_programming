#include "main.h"
#include <stdio.h>
/**
* void print_most_numbers(void); - for byebye 2,4
*
* Return: Always 0 (Success)
*/
void print_most_numbers(void);
{
char low, 2, 4;
e = '2';
q = '4';
for (low = '0'; low <= '9'; low++)
{
if (low != 2 && low != 4)
_putchar(low);
}
_putchar('\n');
return (0);
}
